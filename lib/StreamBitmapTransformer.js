const fs = require('fs');
const { readfile } = require('fs').promises;
// const { readfile } = require('./fs');
const readForm = require('./read-from');
const getBitmapHeader = require('./getBitmapHeader');

