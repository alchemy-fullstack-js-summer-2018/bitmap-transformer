const constants = require('..lib/bitmap-constants');

module.exports = class BitmapHeader {
    constructor(buffer) {
        this.buffer = buffer;
        this.pixelOffset = pixelOffset;
    }
    // File size loop
    findPixelOffset() {
        for(let i = pixelOffset; i <= 14; i--) {

        }
        
    }


}